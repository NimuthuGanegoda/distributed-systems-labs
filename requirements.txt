# Shared dependencies for distributed systems labs/assignments/projects.
# Project-specific extras belong in each assignment/project/lab folder's own requirements.txt.
# See docs/requirements.md for details.
#
# Versions below are the latest release of each package that still supports
# Python 3.9 (the oldest version in the CI test matrix). Newer majors of
# pytest/pytest-asyncio/pylint/black/requests have already dropped 3.9/3.10
# support and would break CI's test (3.9) job if pinned here.

# Testing
pytest>=8.4.2
pytest-asyncio>=1.2.0

# Code Quality
pylint>=3.3.9
black>=25.11.0
flake8>=7.3.0

# Utilities
requests>=2.32.5
numpy>=2.0.2
